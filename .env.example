# Xandeum Pulse - Frontend Configuration
# Frontend reads from Supabase and backend sync service populates the data

# Network
VITE_NETWORK=devnet

# Supabase Configuration (REQUIRED)
# Get these from your Supabase project settings
# Frontend reads all data from Supabase
# Backend sync service populates Supabase with pNode data
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key-here
VITE_ENABLE_HISTORICAL_DATA=true

# API Configuration
VITE_API_TIMEOUT=10000
VITE_POLLING_INTERVAL=30000

# Feature Flags
VITE_ENABLE_WEBSOCKET=false
VITE_ENABLE_NOTIFICATIONS=true
VITE_ENABLE_EXPORT=true

# AI Chatbot Configuration (OPTIONAL)
# The chatbot works without API keys using a local knowledge base
# Adding an API key enables AI-powered responses

# OpenAI Configuration (Recommended)
# Get your API key from: https://platform.openai.com/api-keys
VITE_OPENAI_API_KEY=your-openai-api-key-here

# Google Gemini Configuration (Alternative)
# Get your API key from: https://makersuite.google.com/app/apikey
VITE_GEMINI_API_KEY=your-gemini-api-key-here

# Note: Only one API key is required. The chatbot will:
# 1. Try OpenAI if VITE_OPENAI_API_KEY is set
# 2. Fall back to Gemini if VITE_GEMINI_API_KEY is set
# 3. Use local knowledge base if no API keys are configured
