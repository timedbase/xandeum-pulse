# Xandeum Pulse - Multiple pRPC Endpoints Configuration Example
# Copy this to .env and customize with your endpoints

# Primary pRPC endpoint (your main pNode)
VITE_PRPC_ENDPOINT=http://173.212.203.145:6000/rpc

# Fallback endpoints (comma-separated, no spaces)
# The system will automatically failover to these if the primary fails
VITE_PRPC_FALLBACK_ENDPOINTS=http://192.168.1.101:6000/rpc,http://192.168.1.102:6000/rpc,http://api.xandeum.io:6000/rpc

# Disable mock data to use real endpoints
VITE_USE_MOCK_DATA=false

# Network configuration
VITE_NETWORK=devnet

# Optional: WebSocket endpoint
VITE_PRPC_WS_ENDPOINT=ws://173.212.203.145:6000

# Optional: Supabase for historical data
# VITE_SUPABASE_URL=https://your-project.supabase.co
# VITE_SUPABASE_ANON_KEY=your-anon-key-here
# VITE_ENABLE_HISTORICAL_DATA=true

# Optional: API Configuration
VITE_API_TIMEOUT=10000
VITE_POLLING_INTERVAL=30000

# Optional: Feature Flags
VITE_ENABLE_WEBSOCKET=true
VITE_ENABLE_NOTIFICATIONS=true
VITE_ENABLE_EXPORT=true

# ============================================================
# IMPORTANT NOTES:
# ============================================================
# 1. All pRPC endpoints MUST include the /rpc path
# 2. Use comma separation (no spaces) for multiple fallback endpoints
# 3. The system tries endpoints in order: primary → fallback 1 → fallback 2 → etc.
# 4. Automatic failover happens when an endpoint times out or returns an error
# 5. Restart the dev server after changing this file: npm run dev
# ============================================================

# ============================================================
# EXAMPLE CONFIGURATIONS:
# ============================================================

# Local Development (2 endpoints):
# VITE_PRPC_ENDPOINT=http://192.168.1.100:6000/rpc
# VITE_PRPC_FALLBACK_ENDPOINTS=http://192.168.1.101:6000/rpc

# Production (3 geographic endpoints):
# VITE_PRPC_ENDPOINT=https://prpc-us.xandeum.io/rpc
# VITE_PRPC_FALLBACK_ENDPOINTS=https://prpc-eu.xandeum.io/rpc,https://prpc-asia.xandeum.io/rpc

# High Availability (5 endpoints):
# VITE_PRPC_ENDPOINT=https://prpc-primary.xandeum.io/rpc
# VITE_PRPC_FALLBACK_ENDPOINTS=https://prpc-backup-1.xandeum.io/rpc,https://prpc-backup-2.xandeum.io/rpc,https://prpc-backup-3.xandeum.io/rpc,https://prpc-backup-4.xandeum.io/rpc
